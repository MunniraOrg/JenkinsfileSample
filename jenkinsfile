pipeline {
  agent { label 'Jenkins' }

  triggers {
    cron('H/2 * * * *')
  }
  stages
    {
      stage('Clean workspace and checkout scm') {
        steps {
          echo 'hello world i am in stage'
        }
      }
   }
}
